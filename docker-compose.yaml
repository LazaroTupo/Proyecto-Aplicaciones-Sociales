services:
  db:
    image: mysql:8.0
    container_name: tas_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: tas_db
    ports:
      - "3306:3306"
    volumes:
      - tas_mysql_data:/var/lib/mysql

volumes:
  tas_mysql_data:
